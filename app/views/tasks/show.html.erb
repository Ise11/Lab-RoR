<!--przy scaffold no test framework-->
<!--rspec spec/features/tasks_spec.rb:35-->

<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @task.name %>
</p>

<p>
  <strong>Content:</strong>
  <%= @task.content %>
</p>
<p>
<strong>Dodano: </strong>
<%= @task.created_at %>
</p>

<%= render @task.answers %>

<%= render "answers/form" %>


<%= link_to 'Edit', edit_task_path(@task) %> |
<%= link_to 'Back', tasks_path %>
<%= link_to 'Wczesniejsze', @previous if @previous %>

